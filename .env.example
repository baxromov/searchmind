# SearchMind Environment Configuration
# Copy this file to .env and adjust values as needed

# ============================================================================
# Backend Configuration
# ============================================================================

# Data directories (inside container)
DATA_DIR=/app/data
UPLOAD_DIR=/app/data/uploads
INDEX_DIR=/app/data/faiss_index
TEMP_DIR=/app/data/temp

# Document processing settings
CHUNK_SIZE=400
CHUNK_OVERLAP=80
MAX_FILE_SIZE_MB=50

# Model settings (optional)
# EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# RERANK_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# ============================================================================
# Frontend Configuration
# ============================================================================

# Backend API URL (for frontend to connect)
VITE_API_BASE_URL=http://localhost:8000

# ============================================================================
# Docker Configuration (optional)
# ============================================================================

# Compose project name
COMPOSE_PROJECT_NAME=searchmind

# Build arguments
# PYTHON_VERSION=3.12
# INSTALL_DEV=true

# ============================================================================
# Production Settings (for docker-compose.prod.yml)
# ============================================================================

# Number of uvicorn workers (production)
# WORKERS=4

# Log level
# LOG_LEVEL=info

# Enable/disable reload (development only)
# RELOAD=true

# ============================================================================
# Advanced Configuration
# ============================================================================

# FAISS index settings
# FAISS_INDEX_TYPE=Flat  # Options: Flat, IVF, HNSW
# FAISS_METRIC_TYPE=METRIC_INNER_PRODUCT

# Search settings
# DEFAULT_TOP_K=10
# VECTOR_SEARCH_TOP_K=25

# OCR settings
# OCR_ENABLED=true
# OCR_LANG=en  # Language for OCR

# ============================================================================
# Notes
# ============================================================================

# - Most settings have sensible defaults and don't need to be changed
# - DATA_DIR, UPLOAD_DIR, INDEX_DIR, TEMP_DIR should match docker-compose.yml
# - VITE_API_BASE_URL must be accessible from browser (use localhost for local dev)
# - For production, consider using environment-specific .env files
# - Never commit .env files to version control (they're in .gitignore)
